#ifndef WIFI_CONNECTION_HPP
#define WIFI_CONNECTION_HPP

#include <QObject>

namespace wifi {

class connection {
 public:
  connection();
};

}  // namespace wifi

Q_DECLARE_METATYPE(wifi::connection);

#endif  // WIFI_CONNECTION_HPP
